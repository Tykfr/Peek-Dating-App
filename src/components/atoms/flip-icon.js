import React from "react";
import { StyleSheet } from "react-native";
import { Icon } from "react-native-elements";

const Flip_icon = ({}) => (
  <Icon name="autorenew" type="material" color="#D99202" size={30} />
);
export default Flip_icon;
