import React from "react";
import { StyleSheet } from "react-native";
import { Icon } from "react-native-elements";

const Heart_icon = ({}) => (
  <Icon name="favorite" type="material" color="#D99202" size={50} />
);
export default Heart_icon;
